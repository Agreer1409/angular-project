// An interface is a contract from us and the data. It can help with making sure our code can expect the correct types.
export interface ProjectListings {
  title: string,
  startdate: string,
  enddate: string,
  owner: string,
}
